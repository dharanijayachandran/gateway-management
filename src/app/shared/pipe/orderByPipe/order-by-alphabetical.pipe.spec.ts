import { OrderByAlphabeticalPipe } from './order-by-alphabetical.pipe';

describe('OrderByAlphabeticalPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderByAlphabeticalPipe();
    expect(pipe).toBeTruthy();
  });
});
